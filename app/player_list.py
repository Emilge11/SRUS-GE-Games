from app.player_node import PlayerNode

class PlayerList:
    def __init__(self):
        self._head = None

    def insert_first():
        new_node = PlayerNode()
        if self.is_empty():
            self.head = new_node
    
    def is_empty(self):
        if self.head is None:
            return True
        